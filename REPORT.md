# گزارش حلقه نقد و بازنویسی (۵ مرتبه)

برنامه: **آربیتراژ تتر ایران** — مقایسه قیمت خرید/فروش تتر (USDT/IRT) در صرافی‌های
داخلی و یافتن فرصت‌های آربیتراژ بین‌صرافی‌ای با احتساب کارمزد و کارمزد انتقال.

رویکرد منتقد سختگیر: هر نسخه رو با معیارهای **صحت ریاضی، مقاومت در برابر خطا،
تجربه کاربری موبایل، قابلیت آزمایش (دمو) و آمادگی برای بسته‌بندی APK** ارزیابی کردم.

---

## نسخه ۱ (v1) — نمونه اولیه تک‌رشته‌ای
**نمره منتقد: ۳ / ۱۰**

ایرادات:
1. درخواست‌های شبکه **همگام (blocking)** بود → رابط کاربری کاملاً فریز می‌شد.
2. **کارمزد صرافی‌ها اصلاً لحاظ نمی‌شد**؛ سود = اختلاف قیمت خام در نظر گرفته می‌شد
   (در دنیای واقعی تتر ایران حاشیه‌ها کمتر از کارمزدند → نتیجه کاملاً غلط).
3. **بررسی عمق (volume) نبود** → پیشنهاد معامله ۱۰,۰۰۰ تتر روی صرافی با عمق ۵۰۰ تتر.
4. هیچ **خطایابی** نبود → صرافی مسدود = کرش کل برنامه.
5. **فونت فارسی/RTL** نداشت → روی اندروید متن فارسی مربع‌مربع می‌شد.
6. فقط یک نماد، بدون خلاصه بازار، بدون مقایسه با دلار آزاد.
7. فقط دسکتاپ؛ هیچ تنظیمی برای موبایل و APK.

→ بازنویسی: جدا کردن هسته (`arbitrage_core.py`) از رابط، افزودن کارمزد و ترد.

---

## نسخه ۲ (v2) — افزودن کارمزد، خطایابی و ترد
**نمره منتقد: ۵ / ۱۰**

رفع شد: کارمزد خرید/فروش هر صرافی، `try/except` دور درخواست‌ها، فراخوانی در ترد جداگانه.

ایرادات باقی‌مانده:
1. باز هم **عمق معامله نادیده گرفته می‌شد**.
2. رابط **بدون فونت فارسی** و چیدمان ضعیف.
3. **تنظیمات ذخیره نمی‌شد** (هر بار پیش‌فرض).
4. **حالت دمو نداشت** → بدون اینترنت کاملاً بی‌استفاده.
5. **به‌روزرسانی خودکار** نبود.
6. مقایسه با بازار آزاد نبود.

→ بازنویسی: محاسبه امکان‌پذیری حجم، کارت‌های خلاصه، مقایسه tgju.

---

## نسخه ۳ (v3) — آگاهی از عمق + فونت فارسی + خلاصه + بازار
**نمره منتقد: ۶.۵ / ۱۰**

رفع شد: فیلتر حجم (`amount ≤ min(vol_buy, vol_sell)`)، باندل فونت Vaziranim،
کارت خلاصه (بهترین خرید/فروش)، نمایش حباب تتر نسبت به دلار آزاد.

ایرادات باقی‌مانده:
1. **ذخیره‌سازی تنظیمات** هنوز نبود.
2. **حالت دمو/آفلاین** نبود → توی محیط تست غیرقابل‌استفاده.
3. **به‌روزرسانی خودکار با backoff** نبود (درخواست‌های پشت‌سر هم بدون گارد).
4. endpoint هر صرافی **شکننده** بود (یک فرمت JSON) → تغییر کوچک = خطا.
5. **کش آفلاین** نبود → قطع اینترنت = هیچ دیتایی.
6. فیلتر **حداقل حاشیه** نبود (لیست پر از فرصت‌های بی‌ارزش).

→ بازنویسی: persistence، دمو، کش، فیلتر، پارس چندفرمتی.

---

## نسخه ۴ (v4) — دمو + کش + تنظیمات + فیلتر + خودکار
**نمره منتقد: ۸ / ۱۰**

رفع شد: `settings.json` (ذخیره تنظیمات)، حالت دمو با دیتای نمونه واقعی‌نما،
کش آخرین دیتای موفق (fallback آفلاین)، فیلتر حداقل حاشیه، گارد هم‌زمانی ترد،
پارس دفاعی چندفرمتی (آبجکت/آرایه، price/value).

ایرادات باقی‌مانده:
1. **تنظیمات buildozer ناقص** بود: اجازه اینترنت (`INTERNET`) ست نشده،
   فونت `.ttf` در include_exts نبود، `accept_sdk_license` نبود → APK کار نمی‌کرد.
2. **کتابخانه `certifi`** برای SSL روی اندروید نبود (درخواست‌های HTTPS شکست می‌خورد).
3. **ورژن kivy** پین نشده بود → احتمال عدم تطابق با Cython ساخت.
4. وضعیت هر صرافی **جداگانه نمایش داده نمی‌شد** (کاربر نمی‌دید کدوم آنلاینه).
5. چیدمان ردیف‌ها برای تعداد زیاد صرافی **اسکرول پذیر** نبود.
6. اعداد **فرمت یکنواخت** نداشتند.

→ بازنویسی: اصلاح کامل buildozer.spec، افزودن certifi، پین kivy، نوار وضعیت،
ردیف‌های اسکرول‌پذیر، فرمت‌بندی اعداد.

---

## نسخه ۵ (v5) — نسخه نهایی تولیدی و آماده APK ⭐
**نمره منتقد: ۹ / ۱۰**

این نسخه همه ایرادات نسخه‌های قبل را برطرف کرده:

- ✅ **صحت ریاضی**: سود خالص = (فروش−خرید) − کارمزد خرید − کارمزد فروش −
  کارمزد انتقال شبکه (TRC-20)، با کارمزد **مختص هر صرافی**.
- ✅ **مقاومت**: پارس دفاعی چندفرمتی، `try/except`، کش آفلاین، نمایش خطای
  جداگانه هر صرافی، برگشت به دمو هنگام قطع اینترنت.
- ✅ **موبایل**: فونت فارسی باندل‌شده، چیدمان عمودی اسکرول‌پذیر، کارت‌های خلاصه،
  دکمه تنظیمات، به‌روزرسانی خودکار ۳۰ ثانیه با گارد هم‌زمانی.
- ✅ **قابل آزمایش**: حالت دمو با دیتای واقعی‌نما (حتی بدون اینترنت قابل‌نمایش).
- ✅ **آماده APK**: buildozer.spec اصلاح‌شده (INTERNET، include ttf،
  `accept_sdk_license=true`، `kivy==2.3.0`، `certifi`) + Workflow گیت‌هاب برای ساخت.

نمره ۹ به جای ۱۰ چون (الف) endpointهای چند صرافی ثانویه (اکسیر/رمزینکس/تترلند)
را نتوانستم از این محیط (خارج از ایران) زنده تست کنم و (ب) آربیتراژ مثلثی
(تتر↔بیت‌کوین↔تومن) هنوز پیاده نشده — هر دو در ادامه قابل توسعه‌اند.

---

## خلاصه نمرات
| نسخه | نمره | تمرکز اصلاح |
|------|------|--------------|
| v1 | ۳/۱۰ | جداسازی هسته، کارمزد، ترد |
| v2 | ۵/۱۰ | خطایابی، کارمزد هر صرافی |
| v3 | ۶.۵/۱۰ | عمق معامله، فونت، خلاصه، بازار |
| v4 | ۸/۱۰ | دمو، کش، تنظیمات، فیلتر |
| v5 | ۹/۱۰ | سخت‌سازی تولیدی + آمادگی APK |

**خروجی نهایی:** همین نسخه v5 در قالب **APK** (از طریق GitHub Actions) تحویل داده شده است.
